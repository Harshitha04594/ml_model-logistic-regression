Logistic Regression Classifier 📊

This project implements Logistic Regression using scikit-learn to classify data into categories.
It is one of the simplest and most widely used algorithms for binary classification problems (e.g., predicting if a student passes/fails, if an email is spam/not spam, etc.).

🚀 Features

Data cleaning & preprocessing

Train-test split for evaluation

Logistic Regression model training
